{
  "name": "low-poly-fps",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "server": "npm run start --prefix server",
    "dev:all": "concurrently -n web,ws -c cyan,magenta \"npm run dev\" \"npm run server\""
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
